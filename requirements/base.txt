# Núcleo da aplicação
Django>=6.1,<6.2
djangorestframework>=3.18
djangorestframework-simplejwt>=5.5
drf-spectacular>=0.30
django-cors-headers>=4.9
django-environ>=0.14
dj-database-url>=3.1
psycopg[binary]>=3.2

# Tarefas assíncronas / agendadas (relatório educacional mensal)
#
# Sem django-celery-beat: ele guarda o agendamento no banco, para ser editado
# pelo admin, e o nosso é fixo em config/celery.py. O pacote nunca esteve em
# INSTALLED_APPS nem foi importado — só constava aqui, travando a instalação
# por exigir Django < 6.1.
celery>=5.5
redis>=5.2

# Integrações
anthropic>=0.40          # módulo educacional (IA)
httpx>=0.28              # API do Banco Central (SGS: Selic/IPCA)
google-auth>=2.35        # valida o token do "Entrar com o Google"
requests>=2.32           # transporte HTTP exigido pelo google-auth acima

# Geração de PDF — Python puro, sem bibliotecas nativas
reportlab>=4.2

# Servidor de aplicação
gunicorn>=23.0
whitenoise>=6.8
stripe>=11.0            # SDK oficial; o mock funciona sem ele instalado

# Monitoramento de erro. Fica inerte sem SENTRY_DSN configurado, então não
# muda nada em desenvolvimento nem em teste.
sentry-sdk[django]>=2.18
